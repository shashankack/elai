# =============================================================================
# Admin panel — environment variables
# Copy to .env and fill in your values:
#   cp .env.example .env
# =============================================================================

# Backend API URL (optional — defaults to http://localhost:9000 in dev)
VITE_MERCUR_BACKEND_URL=http://localhost:9000

# Vendor panel URL (used for seller invite links)
VITE_MERCUR_VENDOR_URL=http://localhost:7001
